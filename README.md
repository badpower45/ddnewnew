
  # CHANCE Platform Design Architecture

  This is a code bundle for CHANCE Platform Design Architecture. The original project is available at https://www.figma.com/design/WSX6XBiP0SmxG2WTgJt3nR/CHANCE-Platform-Design-Architecture.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # ddnewnew
